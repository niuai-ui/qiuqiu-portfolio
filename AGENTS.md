# 湫湫 Sims 汉化档案馆

这是一个完全静态、托管于 GitHub Pages 的个人汉化作品集。网站内容以项目最外层的 `作品信息.xlsx` 为唯一数据源，由 `tools/build_site.py` 生成。

## Excel 固定结构

“作品信息”工作表只允许以下 13 列，顺序固定：

1. 状态
2. 模组英文名
3. 模组中文名
4. 原作者名字
5. 原作者网址链接
6. 模组本体链接
7. 汉化发布日期
8. 汉化更新日期
9. 前置说明
10. 放置说明
11. 封面路径
12. 百度网盘链接整体
13. 类别

只有状态为“已发布”的作品会出现在网站。百度网盘地址与提取码写在同一个单元格中；源文件里没有地址时保留空白，不得编造。

类别只使用：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。

## 从日志目录同步

需要同步时，扫描 `E:\自媒体 湫湫sims日志\功能模组`：

- 忽略 `待发布`，其中是未汉化或准备汉化的内容。
- 忽略 `【飞书作品集】`，其中是待删除的作废内容。
- 直接位于“功能模组”下的模组填写“必须放第一层”。
- 位于“功能模组\其他功能模组”下的模组填写“无需放第一层”。
- 从模组文件夹中的 `无需前置.txt` 或 `模组前置：*.txt` 提取前置说明。
- 文件夹名通常为“作者 - 英文模组名 - 中文名 - 日期尾码”；最后的四位日期尾码不录入。
- 封面优先选择“作者自留”中名字带 `(1)` 的图片；没有 `(1)` 时再选择其他 3:4 图片。
- 封面复制到 `content/images`，Excel 中填写 `images/文件名.png`。封面必须为完整 3:4，网页不得裁掉一半。构建器会自动生成 480/960 像素 WebP，手机端加载小图，详情页加载大图；不要把 Excel 改成 WebP 路径。
- 新作品的“汉化发布日期”和“汉化更新日期”初始相同。已有作品更新时保留原发布日期，只修改更新日期。
- 原作者网址优先使用作者官方网站、Patreon 或作者主页，并由网站将“原作者名字”渲染成可点击链接。
- “模组本体链接”只填写该模组自己的官方发布页，不得填写作者主页、搜索结果页或猜测地址；找不到时保留空白。网站详情弹窗将其渲染成“模组本体”链接。
- 新增或更新 Excel 记录时，必须完整复制现有标准数据行的单元格格式（字体、填充、边框、对齐、日期格式、自动换行）并保持数据行高度为 36；不得把无格式的默认单元格直接追加到表尾。`tools/preflight.py` 会自动拦截格式不一致的行。
- 同步时比较现有 Excel，只新增或更新发生变化的模组，不重复创建旧记录。

网站分类导航固定显示：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。

## 发布与校验

1. 运行 `python tools/preflight.py`，统一检查日志源目录与 Excel 是否一一对应，以及 13 列固定结构、数据行格式、必填字段、合法类别、真实日期、URL、精确 3:4 封面、静态构建、Python 和 JavaScript 语法。日志同步仅在本机检查，因为 GitHub Actions 无法访问日志盘；只需快速核对同步时可单独运行 `python tools/check_sync.py`。
2. 检查详情弹窗：左侧完整 3:4 图片，右侧显示作者链接、模组本体链接、繁简汉化、前置、放置、发布日期、更新日期和下载入口。
3. 发布前暂存区必须为空；只暂存作品集相关路径，不要提交 `outputs/` 或临时预览文件。
4. 推送 `main` 后等待对应提交的 GitHub Pages 工作流成功。

日常发布优先双击 `更新作品集.cmd`；该脚本会按上述顺序执行检查、提交、推送和部署等待，任何检查失败都不会提交或上传。

`content/images/favicon.png` 是浏览器标签页图标；`content/images/og.jpg` 是社交平台分享预览图，二者不是作品封面，不写入 Excel。
