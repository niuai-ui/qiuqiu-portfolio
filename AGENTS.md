# 湫湫 Sims 汉化档案馆

这是一个完全静态、托管于 GitHub Pages 的个人汉化作品集。网站内容以项目最外层的 `作品信息.xlsx` 为唯一数据源，由 `tools/build_site.py` 生成。

## WorkBuddy 任务记录

- 每次开始整体优化、工作流迭代、批量同步或全项目审查前，必须先读取 `.workbuddy/memory/MEMORY.md`，并查看 `.workbuddy/memory` 中最近 3～5 份按日期记录的任务日志。
- WorkBuddy 记录用于了解近期批量工作的实际过程、问题和可复用经验，但它只是历史上下文；其中涉及路径、运行时、脚本状态、文件现状或发布结果的内容，必须与当前文件系统、Git 状态和实际检查结果交叉验证。过时记录不得覆盖当前事实。
- 优化工作流时，应结合近期记录判断是否存在反复操作、遗漏检查、失效步骤或可安全精简的环节；没有明确收益时不为了改动而改动。

## WorkBuddy Skill 管理

- 本项目的长期业务规则只维护在 `AGENTS.md`；近期任务事实写入 `.workbuddy/memory`。不得为了重复保存已有项目规则而频繁生成 Skill。
- 网站发布已有唯一的项目级 WorkBuddy Skill：`.codebuddy/skills/sims4-portfolio-publish/`。新窗口必须优先复用这一份，不得为相同用途创建近义、改名或重复 Skill。
- 项目专用 Skill 只允许存放在本项目 `.codebuddy/skills/` 并随 Git 管理；项目专用辅助脚本放在项目 `tools/`。不得把本项目专用 Skill 或脚本写入 WorkBuddy 用户级目录。用户级 Skill 只保留真正跨项目通用的能力。
- 只有确实会跨多次任务稳定复用、且不适合放入项目规则或脚本的 WorkBuddy 专用操作方法，才可以考虑写入 Skill；创建新的项目相关 Skill 前必须先盘点现有项目级 Skill，并获得用户明确同意。批准后也必须创建在 `.codebuddy/skills/`，不得创建为用户级 Skill。
- Skill 不能覆盖 `AGENTS.md`。两者不一致时以当前项目文件、用户最新要求和 `AGENTS.md` 为准，并及时修正旧 Skill，避免双份规则继续漂移。
- WorkBuddy 在本项目中是规则执行与问题反馈端。每次任务必须先完整读取并严格遵守 `AGENTS.md`；网站上新、资料修改、网盘更新、画廊同步和发布统一使用现有 `sims4-portfolio-publish`，并在使用前读取其完整 `SKILL.md`。
- 对这个个人作品集网站，默认直接相关的 WorkBuddy 项目 Skill 只有 `sims4-portfolio-publish`。`sims4-author-curator`、`curseforge-file-download`、`sims4-mod-cleanup`、`sims4-mods-flatten` 和 `agent-browser-core` 属于其他或上游工作流，不得因为同在用户级 Skill 目录就自动带入本项目；只有用户明确提出相应范围的任务时才可另行使用。
- WorkBuddy 不得以“优化流程”为由自行修改 `AGENTS.md`、新建或修改 Skill，或改写与当前业务任务无关的工作流脚本。若规则或工具出现问题，应优先保证当前任务安全；无法按规则继续时停止相关操作并向用户说明，不得自行绕开安全边界。
- WorkBuddy 遇到流程缺陷、重复坑、规则歧义、工具故障或可能需要 Skill 的需求时，必须在当天 `.workbuddy/memory/YYYY-MM-DD.md` 追加“工作流问题反馈”，记录：当前任务、已遵循的规则或 Skill、问题与复现条件、临时处理及其风险、对结果的影响、相关文件或命令证据、建议整改方向、是否属于 Skill 候选。记录反馈不等于授权创建 Skill。
- 后续整体 review 或工作流优化时，Codex 必须检查近期日志中的“工作流问题反馈”和 Skill 候选，统一判断应当修改 `AGENTS.md`、修补项目 `tools/` 脚本、更新已有 Skill、仅保留为经验记录，还是确需新建 Skill；完成整改和验证后再提交发布。只有确需新建 Skill 时才向用户说明理由并取得同意。

## Excel 固定结构

“作品信息”工作表只允许以下 14 列，顺序固定：

1. 状态
2. 模组英文名
3. 模组中文名
4. 原作者名字
5. 原作者网址链接
6. 模组本体链接
7. 模组更新日期
8. 汉化更新日期
9. 前置说明
10. 放置说明
11. 封面路径
12. 百度网盘链接整体
13. 类别
14. 上新日期

只有状态为“已发布”的作品会出现在网站。百度网盘地址与提取码写在同一个单元格中；源文件里没有地址时保留空白，不得编造。

类别只使用：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。

## 从日志目录同步

需要同步时，扫描 `E:\自媒体 湫湫sims日志\功能模组`：

- 忽略 `待发布`，其中是未汉化或准备汉化的内容。
- 忽略 `【飞书作品集】`，其中是待删除的作废内容。
- 直接位于“功能模组”下的模组填写“必须放第一层”。
- 位于“功能模组\其他功能模组”下的模组填写“无需放第一层”。
- 从模组文件夹中的 `无需前置.txt` 或 `模组前置：*.txt` 提取前置说明。
- 文件夹名通常为“作者 - 英文模组名 - 中文名 - 日期尾码”；最后的四位日期尾码不录入。
- 封面优先选择“作者自留”中名字带 `(1)` 的图片；没有 `(1)` 时再选择其他 3:4 图片。
- 封面复制到 `content/images`，Excel 中填写 `images/文件名.png`。封面必须为完整 3:4，网页不得裁掉一半。构建器会自动生成 480/960 像素 WebP，手机端加载小图，详情页加载大图；不要把 Excel 改成 WebP 路径。
- 每个模组的 `模组介绍` 文件夹必须至少有一张介绍图。运行 `python tools/sync_gallery.py` 将这些图片按文件名自然顺序转换为最长边 1600 像素的 WebP，并同步到 `content/gallery`；详情弹窗将封面作为第 1 张，介绍图依次向后翻页。不得手工猜测或打乱图片归属。
- “上新日期”记录该作品首次加入网站的日期，创建后永远不因模组或汉化更新而改变。“模组更新日期”记录原版模组最近一次更新，“汉化更新日期”只记录汉化文件最近一次更新。新作品的三个日期初始相同。原版模组更新但汉化内容不变时，只修改“模组更新日期”；只有汉化文件发生变化时才修改“汉化更新日期”。三个日期彼此独立。
- 原作者网址优先使用作者官方网站、Patreon 或作者主页，并由网站将“原作者名字”渲染成可点击链接。
- “模组本体链接”只填写该模组自己的官方发布页，不得填写作者主页、搜索结果页或猜测地址；找不到时保留空白。网站详情弹窗将其渲染成“模组本体”链接。
- 新增或更新 Excel 记录时，必须完整复制现有标准数据行的单元格格式（字体、填充、边框、对齐、日期格式、自动换行）并保持数据行高度为 36；不得把无格式的默认单元格直接追加到表尾。`tools/preflight.py` 会自动拦截格式不一致的行。
- 同步时比较现有 Excel，只新增或更新发生变化的模组，不重复创建旧记录。

网站分类导航固定显示：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。

网站排序固定提供：新模组上新（按“上新日期”）、模组最近更新（按“模组更新日期”）、汉化最近更新（按“汉化更新日期”）、名称排序。更新日历按这三个日期自动生成；同一天内分别标记“新模组上新”“模组本体更新”“汉化文件更新”。模组本体更新而汉化日期未变化时，日志必须明确显示“汉化文件未变化”。更新日历必须按月分页，每月固定显示 7 列 × 6 行的 42 个日期格和固定行高；无动态日期保留空格。有动态日期时，模组名称使用正常字号在日期格内横向紧凑平铺并自动换行，独立小框本身作为分隔，不额外保留明显间距；每个小框均可点击并直接打开对应模组详情。小框颜色必须与动态类型一致：上新为橙色、模组本体更新为绿色、汉化文件更新为紫色；同日同时更新本体与汉化时使用绿紫双色。实际测量后仍超出两行时才显示“更多”，不得为了硬塞内容缩小字号或加高日期格；点击“更多”后使用浅色弹框完整显示当日的全部模组小框。日历不得再显示重复内容的黑色悬停详情框。手机端保留可点击的模组小框，通过横向滑动查看完整 7 列日历，点击普通小框直接打开模组详情，点击“更多”只打开当日全部小框。

## 发布与校验

1. 运行 `python tools/sync_gallery.py` 同步介绍图，再运行 `python tools/preflight.py`，统一检查日志源目录与 Excel 是否一一对应，以及 14 列固定结构、数据行格式、必填字段、合法类别、真实日期、URL、精确 3:4 封面、介绍图同步、静态构建、Python 和 JavaScript 语法。日志同步仅在本机检查，因为 GitHub Actions 无法访问日志盘；只需快速核对同步时可单独运行 `python tools/check_sync.py`。
2. 检查详情弹窗：左侧完整 3:4 封面且可向后翻看介绍图，右侧显示作者链接、模组本体链接、繁简汉化、前置、放置、上新日期、模组更新日期、汉化更新日期和下载入口。
3. 发布前暂存区必须为空；只暂存作品集相关路径，不要提交 `outputs/` 或临时预览文件。
4. 推送 `main` 后等待对应提交的 GitHub Pages 工作流成功。

日常发布优先双击 `更新作品集.cmd`；该脚本会按上述顺序执行检查、提交、推送和部署等待，任何检查失败都不会提交或上传。

`content/images/favicon.png` 是浏览器标签页图标；`content/images/og.jpg` 是社交平台分享预览图，二者不是作品封面，不写入 Excel。
