# 湫湫 Sims 汉化档案馆

本项目是托管于 GitHub Pages 的静态汉化作品集。

- 唯一内容数据源：根目录 `作品信息.xlsx`
- 网站源码：`site/`
- 作品素材：`content/`
- 构建与校验脚本：`tools/`
- 自动生成目录：`dist/`（不得提交）
- Codex 与 WorkBuddy 共用且唯一的项目规则入口：本文件 `AGENTS.md`
- 网站发布 Skill：`.codebuddy/skills/sims4-portfolio-publish/SKILL.md`

## 1. 先明确职责

### WorkBuddy：执行与反馈

WorkBuddy 必须严格按本文件和项目 Skill 执行用户任务，只修改本次任务涉及的字段与文件。完成修改后负责同步、预检、提交、推送、等待部署和线上核对。

WorkBuddy 不得自行：

- 修改 `AGENTS.md`、项目 Skill 或与当前任务无关的工作流脚本。
- 新建、复制、改名或迁移 Skill。
- 根据旧日志、历史行号或猜测修改数据。
- 顺手修改用户未要求的日期、类别、链接或其他字段。

遇到规则缺口、工具故障或可能需要 Skill 的情况，只记录问题并向用户说明，不擅自整改工作流。

### Codex：审查与治理

Codex 负责整体 review、规则整理、脚本优化和 Skill 治理。开始整体优化、批量同步、工作流迭代或全项目审查前，必须读取：

1. `.workbuddy/memory/MEMORY.md`
2. `.workbuddy/memory` 中最近 3～5 份日期日志
3. 日志中的“工作流问题反馈”和 Skill 候选

Codex 决定问题应修改规则、修补脚本、更新现有 Skill，还是仅保留为历史经验。确需新建 Skill 时，必须先向用户说明理由并取得同意。

### 规则冲突时的优先级

1. 用户当前任务中的最新明确要求
2. `AGENTS.md`
3. `.codebuddy/skills/sims4-portfolio-publish/SKILL.md`
4. 当前文件系统、Git 状态和脚本实际检查结果
5. `.workbuddy/memory` 历史记录

历史记录只提供上下文，过时内容不得覆盖当前事实。

## 2. 每次任务统一按此流程执行

### 开始前

1. 完整读取 `AGENTS.md`。
2. 完整读取 `.codebuddy/skills/sims4-portfolio-publish/SKILL.md`，不得因新窗口或措辞不同另建 Skill。
3. 运行 `git status --short`：
   - 工作区干净：先运行 `git pull --ff-only origin main`。
   - 已有修改：先确认归属，保留并避开用户的无关修改。
   - 暂存区已有内容：先查明来源，不得与本次任务混合提交。
4. 明确目标作品、本次允许修改的字段、必须保持不变的字段和需要发布的文件。
5. 从当前日志源目录与 Excel 双向核对，不依赖旧日志中的行号或结论。

### 修改后

1. 回读并核对本次修改值；Excel 还要核对格式和行高。
2. 运行 `python tools/sync_gallery.py`。
3. 运行 `python tools/preflight.py`。
4. 核对详情弹窗和本次目标数据；只需快速检查日志与 Excel 时可运行 `python tools/check_sync.py`。
5. 任一检查失败时，不得提交或推送。

### 提交与发布

用户要求修改或发布时，检查通过后直接完成发布，不要求用户执行额外脚本：

1. 确认暂存区原本为空。
2. 只 `git add` 本次实际改动的精确路径。禁止 `git add .`，也禁止笼统暂存整个 `content/`、`site/` 或 `tools/`。
3. 运行 `git diff --cached --name-status` 和 `git diff --cached --check`，排除无关文件和格式错误。
4. 使用准确的提交信息，提交并推送 `main`。
5. 用 `git rev-parse HEAD` 取得完整 40 位 SHA。
6. 运行 `python tools/wait_for_pages.py <完整 SHA>` 等待对应 Pages 工作流成功。
7. 给线上 `data.json` 添加时间戳查询参数，程序化核对目标条目、字段和总数，避免 CDN 缓存误判。
8. 运行 `git status --short`，确认工作区干净。

只有 Pages 成功且线上数据正确后，才能向用户表述“已发布”。不得提交 `dist/`、`outputs/`、临时预览、工作簿备份、缓存或其他任务无关文件。

## 3. Excel 固定数据规则

“作品信息”工作表只能有以下 14 列，名称与顺序固定：

1. 状态
2. 模组英文名
3. 模组中文名
4. 原作者名字
5. 原作者网址链接
6. 模组本体链接
7. 模组更新日期
8. 汉化更新日期
9. 前置说明
10. 放置说明
11. 封面路径
12. 百度网盘链接整体
13. 类别
14. 上新日期

### 通用要求

- 只有状态为“已发布”的作品出现在网站。
- 类别只允许：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。
- 用“模组英文名 + 模组中文名”定位记录，不依赖历史行号。
- 同步前比较现有记录，只新增或更新真实发生变化的作品，不重复创建旧记录。
- 新增行必须完整复制现有标准行的字体、填充、边框、对齐、日期格式和自动换行，行高固定为 36。
- 修改现有行后不得破坏其格式和行高。
- 百度网盘地址和提取码写在同一单元格：`链接: <url> 提取码: <code>`。源文件没有地址时留空，不得编造。
- 原作者网址优先填写作者官网、Patreon 或作者主页。
- 模组本体链接只填写该模组自己的官方发布页；不得填写作者主页、搜索页或猜测地址，找不到时留空。

### 三个日期彼此独立

- 上新日期：作品首次加入网站的日期，创建后永不改变。
- 模组更新日期：原版模组最近一次更新的日期。
- 汉化更新日期：汉化文件最近一次实际变化的日期。

常见任务速查：

- 新作品首次加入：三个日期初始相同。
- 只更新原版：只改“模组更新日期”，日志注明“汉化文件未变化”。
- 只更新汉化：只改“汉化更新日期”。
- 只补链接、类别或说明：三个日期都不改。

除非用户明确要求且符合上述定义，不得把“今天处理”理解成修改日期。

## 4. 从日志源目录同步

日志源目录：`E:\自媒体 湫湫sims日志\功能模组`

### 扫描与放置说明

- 忽略 `待发布`：其中是未汉化或准备汉化的内容。
- 忽略 `【飞书作品集】`：其中是待删除的作废内容。
- 直接位于“功能模组”下：填写“必须放第一层”。
- 位于“功能模组\其他功能模组”下：填写“无需放第一层”。
- 文件夹名通常为“作者 - 英文模组名 - 中文名 - 日期尾码”；末尾四位日期尾码不录入 Excel。

### 前置、封面与介绍图

- 从 `无需前置.txt` 或 `模组前置：*.txt` 提取前置说明；文件内容为空时，前置名称可能写在文件名中。
- 封面优先选择“作者自留”中名称带 `(1)` 的图片；没有 `(1)` 时再选择其他精确 3:4 图片。
- 封面复制到 `content/images`，Excel 填写 `images/文件名.png`，不得填写构建生成的 WebP 路径。
- 封面必须保留完整 3:4 内容，不得裁掉一半。构建器会生成 480/960 像素 WebP。
- 每个模组的 `模组介绍` 文件夹至少有一张介绍图。
- `tools/sync_gallery.py` 按文件名自然顺序将介绍图转换为最长边 1600 像素的 WebP，并同步到 `content/gallery`；不得手工猜测归属或改乱顺序。
- 详情弹窗第 1 张是封面，后续才是介绍图。

## 5. 网站展示不可破坏

### 导航、排序与详情

- 分类导航固定为：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。
- 排序固定为：新模组上新、模组最近更新、汉化最近更新、名称排序。
- 详情弹窗左侧显示完整 3:4 封面并可翻看介绍图。
- 详情弹窗右侧显示作者链接、模组本体链接、繁简汉化、前置、放置、上新日期、模组更新日期、汉化更新日期和下载入口。
- `content/images/favicon.png` 是标签页图标，`content/images/og.jpg` 是分享预览图；二者不是作品封面，不写入 Excel。

### 更新日历

- 日历分别依据上新日期、模组更新日期和汉化更新日期生成“新模组上新”“模组本体更新”“汉化文件更新”。
- 颜色固定：上新为橙色、本体更新为绿色、汉化更新为紫色；同日本体与汉化都更新时使用绿紫双色。
- 每月固定 7 列 × 6 行，共 42 个日期格，行高固定；无动态日期保留空格。
- 模组名称使用正常字号、横向紧凑排列并自动换行；独立小框本身作为分隔，不额外留明显间距。
- 实际测量后仍超过两行时才显示“更多”；不得通过缩小字号或加高日期格硬塞内容。
- 点击普通小框直接打开模组详情；点击“更多”使用浅色弹框显示当日全部模组。
- 不显示重复内容的黑色悬停框。
- 手机端通过横向滑动查看完整 7 列日历，小框仍可点击。

## 6. 校验工具与故障处理

`tools/preflight.py` 统一检查：日志源与 Excel 一致性、介绍图同步、14 列结构、数据行格式、必填字段、合法类别、真实日期、URL、精确 3:4 封面、静态构建、Python 和 JavaScript 语法。GitHub Actions 无法访问本机日志盘，因此只运行 `--skip-sync`。

如 WorkBuddy 的 safe-delete 钩子误拦截旧 `dist/`，只允许执行：

`python tools/purge_dist.py "E:\模拟人生4 湫湫Sims日志作品集网站\dist"`

随后必须重新运行完整 preflight。该脚本不得用于其他路径，也不得复制后用于清理其他目录。

## 7. Skill、脚本与问题记录

- 本网站唯一项目级 Skill：`.codebuddy/skills/sims4-portfolio-publish/`。
- 项目专用 Skill 只放在 `.codebuddy/skills/`；项目专用辅助脚本只放在 `tools/`，均随 Git 管理。
- 不得在 WorkBuddy 用户级目录保存本项目专用 Skill 或脚本副本。
- Skill 只补充执行方法，不复制或覆盖本文件的业务规则；不一致时以 `AGENTS.md` 为准，由 Codex 统一修正。
- 其他 Sims Skill 不属于本网站流程，除非用户明确扩展任务范围，否则不得自动使用。

WorkBuddy 发现流程缺陷、重复坑、规则歧义、工具故障或 Skill 候选时，在当天 `.workbuddy/memory/YYYY-MM-DD.md` 追加：

```text
## 工作流问题反馈：<简短名称>
- 当前任务：
- 已遵循的规则或 Skill：
- 问题与复现条件：
- 临时处理及其风险：
- 对结果的影响：
- 相关文件、命令或错误证据：
- 建议整改方向：
- 是否属于 Skill 候选：是 / 否 / 不确定
```

记录问题不等于获得修改规则、绕开安全措施或创建 Skill 的授权。无法在现有规则内安全完成时，应停止相关操作并向用户说明。
