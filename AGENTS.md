# 湫湫 Sims 汉化档案馆

这是一个完全静态、托管于 GitHub Pages 的个人汉化作品集。项目根目录的 `作品信息.xlsx` 是网站内容的唯一数据源，`tools/build_site.py` 负责生成静态网站。根目录的 `AGENTS.md` 是 Codex 与 WorkBuddy 共用且唯一的项目规则入口。

## 1. 规则优先级与职责

### 1.1 规则优先级

按以下顺序处理冲突：

1. 用户在当前任务中的最新明确要求。
2. 本文件 `AGENTS.md`。
3. 项目级 Skill：`.codebuddy/skills/sims4-portfolio-publish/SKILL.md`。
4. 项目脚本的实际校验结果。
5. `.workbuddy/memory` 中的历史记录。

历史日志只提供上下文，不能覆盖当前文件、Git 状态、实际命令结果或本规则。

### 1.2 WorkBuddy 的职责

WorkBuddy 是本项目的规则执行与问题反馈端，必须：

- 先读规则和项目 Skill，再执行任务。
- 只修改用户本次要求涉及的文件和字段。
- 完成同步、预检、提交、推送、部署等待和线上复核。
- 遇到流程问题时记录证据，不擅自改变治理规则。

WorkBuddy 不得以“优化”为由自行修改 `AGENTS.md`、项目 Skill，或改写与当前业务任务无关的工作流脚本；不得自行新建 Skill。无法在现有规则内安全继续时，停止相关操作并向用户说明。

### 1.3 Codex 的职责

Codex 负责整体 review、工作流优化和规则治理。每次整体优化、批量同步、工作流迭代或全项目审查前，必须读取：

- `.workbuddy/memory/MEMORY.md`
- `.workbuddy/memory` 中最近 3～5 份日期日志
- 日志里的“工作流问题反馈”和 Skill 候选

Codex 负责判断问题应通过修改规则、修补 `tools/`、更新已有 Skill、保留为经验记录，还是确需新建 Skill；只有确需新建 Skill 时才向用户说明理由并取得同意。

## 2. WorkBuddy 每次任务的强制启动步骤

处理本网站的上新、资料修改、链接更新、画廊同步或发布任务时，必须依次执行：

1. 完整读取 `AGENTS.md`。
2. 完整读取 `.codebuddy/skills/sims4-portfolio-publish/SKILL.md`。
3. 运行 `git status --short`，确认工作区和暂存区状态；保留并避开用户已有的无关修改。
4. 明确本次目标作品、允许修改的字段、应保持不变的字段和需要发布的文件。
5. 只依据当前源文件、Excel 和实际检查结果操作，不凭历史行号、旧日志或记忆猜测。

若暂存区已有内容，必须先查明来源；不得把既有暂存内容和本次任务混在同一个提交中。

## 3. 项目 Skill 与脚本管理

- 本网站唯一的项目级 WorkBuddy Skill 是 `.codebuddy/skills/sims4-portfolio-publish/`。
- 项目专用 Skill 只放在 `.codebuddy/skills/` 并随 Git 管理。
- 项目专用辅助脚本只放在项目 `tools/`。
- 不得在 WorkBuddy 用户级目录保存本项目专用 Skill 或脚本的副本；用户级 Skill 只用于真正跨项目通用的能力。
- 不得为同一用途创建近义、改名或重复 Skill。新建项目 Skill 必须先获得用户明确同意，并创建在 `.codebuddy/skills/`。
- Skill 只补充执行方法，不能复制或覆盖本文件的业务规则。两者不一致时以本文件为准，并由 Codex 统一修正 Skill。
- 其他 Sims Skill 属于其他或上游工作流，只有用户明确把任务扩展到相应范围时才能使用，不能自动带入本网站任务。

## 4. Excel 数据契约

“作品信息”工作表只允许以下 14 列，名称与顺序固定：

1. 状态
2. 模组英文名
3. 模组中文名
4. 原作者名字
5. 原作者网址链接
6. 模组本体链接
7. 模组更新日期
8. 汉化更新日期
9. 前置说明
10. 放置说明
11. 封面路径
12. 百度网盘链接整体
13. 类别
14. 上新日期

数据规则：

- 只有状态为“已发布”的作品出现在网站。
- 类别只允许：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。
- 百度网盘地址和提取码写在同一单元格，格式为 `链接: <url> 提取码: <code>`；源文件没有地址时留空，不得编造。
- 原作者网址优先使用作者官网、Patreon 或作者主页。
- 模组本体链接只填写该模组自己的官方发布页；不得填写作者主页、搜索结果页或猜测地址，找不到时留空。
- 按“模组英文名 + 模组中文名”定位记录，不依赖历史行号。
- 新增或更新记录时，完整复制现有标准数据行的字体、填充、边框、对齐、日期格式和自动换行，数据行高度保持 36。
- 同步前比较现有 Excel，只新增或更新真实发生变化的作品，不重复创建旧记录，不顺手修改无关字段。

### 4.1 三个日期彼此独立

- 上新日期：作品首次加入网站的日期，创建后永远不因后续更新改变。
- 模组更新日期：原版模组最近一次更新的日期。
- 汉化更新日期：只有汉化文件实际变化时才更新。
- 新作品首次加入时，三个日期初始相同。
- 只更新原版时，只改模组更新日期，并在日志说明“汉化文件未变化”。
- 只补网盘链接、官方链接、分类或说明时，不改任何日期，除非用户明确要求且符合上述定义。

## 5. 从日志源目录同步

同步源目录：`E:\自媒体 湫湫sims日志\功能模组`

### 5.1 扫描范围

- 忽略 `待发布`，其中是未汉化或准备汉化的内容。
- 忽略 `【飞书作品集】`，其中是待删除的作废内容。
- 直接位于“功能模组”下：放置说明填写“必须放第一层”。
- 位于“功能模组\其他功能模组”下：放置说明填写“无需放第一层”。
- 文件夹名通常为“作者 - 英文模组名 - 中文名 - 日期尾码”；末尾四位日期尾码不录入 Excel。

### 5.2 前置、封面与介绍图

- 从 `无需前置.txt` 或 `模组前置：*.txt` 提取前置说明；文件内容为空时，前置名称可能在文件名中。
- 封面优先选“作者自留”中名称带 `(1)` 的图片；没有 `(1)` 时再选其他精确 3:4 图片。
- 封面复制到 `content/images`，Excel 仍填写 `images/文件名.png`，不得改成生成后的 WebP 路径。
- 封面必须保留完整 3:4 内容，网页不得裁掉一半。构建器生成 480/960 像素 WebP，手机加载小图，详情页加载大图。
- 每个模组的 `模组介绍` 文件夹至少有一张介绍图。
- 使用 `python tools/sync_gallery.py` 按文件名自然顺序同步介绍图到 `content/gallery`，最长边转换为 1600 像素 WebP；不得手工猜测、改序或混淆图片归属。
- 详情弹窗的第 1 张是封面，后续才是介绍图。

## 6. 网站不可破坏的展示规则

### 6.1 导航、排序与详情

- 分类导航固定为：人物特征、用地特征、职业、覆盖替换、游戏玩法、其他。
- 排序固定提供：新模组上新、模组最近更新、汉化最近更新、名称排序。
- 详情弹窗左侧显示完整 3:4 封面并可翻看介绍图。
- 详情弹窗右侧显示作者链接、模组本体链接、繁简汉化、前置、放置、上新日期、模组更新日期、汉化更新日期和下载入口。
- `content/images/favicon.png` 是标签页图标，`content/images/og.jpg` 是分享预览图；二者不是作品封面，不写入 Excel。

### 6.2 更新日历

- 日历依据上新日期、模组更新日期和汉化更新日期自动生成。
- 同一天分别标记“新模组上新”“模组本体更新”“汉化文件更新”。
- 颜色固定：上新为橙色、本体更新为绿色、汉化更新为紫色；同日本体与汉化都更新时使用绿紫双色。
- 每月固定 7 列 × 6 行，共 42 个日期格，行高固定；无动态日期保留空格。
- 模组名称使用正常字号、横向紧凑平铺并自动换行，独立小框作为分隔，不额外留明显间距。
- 实际测量后仍超过两行时才显示“更多”；不得缩小字号或加高日期格硬塞内容。
- 点击普通小框直接打开对应模组详情；点击“更多”使用浅色弹框显示当日全部模组小框。
- 不显示重复内容的黑色悬停框。
- 手机端保留可点击小框，通过横向滑动查看完整 7 列日历。

## 7. WorkBuddy 标准执行与发布流程

### 7.1 修改前

1. 按第 2 节完成强制启动检查。
2. 工作区干净时先执行 `git pull --ff-only origin main`；工作区已有修改时先识别归属，不得覆盖、丢弃或混入本次提交。
3. 从源目录和 Excel 双向核对目标作品，仅修改用户要求涉及的字段和文件。

### 7.2 修改后检查

1. 运行 `python tools/sync_gallery.py`。
2. 运行 `python tools/preflight.py`。
3. 如 WorkBuddy 的 safe-delete 钩子误拦截旧 `dist/`，只允许运行：

   `python tools/purge_dist.py "E:\模拟人生4 湫湫Sims日志作品集网站\dist"`

   随后必须重新运行完整 preflight。该脚本不得用于任何其他路径。
4. 检查详情弹窗和本次目标数据；必要时运行 `python tools/check_sync.py` 做快速同步核对。
5. 任一检查失败时不得提交或推送。

`preflight.py` 统一检查：日志源与 Excel 一致性、介绍图同步、14 列结构、数据行格式、必填字段、合法类别、真实日期、URL、精确 3:4 封面、静态构建、Python 与 JavaScript 语法。GitHub Actions 因无法访问本机日志盘，只运行 `--skip-sync`。

### 7.3 提交与发布

用户要求修改或发布时，WorkBuddy/Codex 完成修改后直接执行以下发布链路：

1. 确认暂存区在本次暂存前为空。
2. 只 `git add` 本次实际改动的精确路径；禁止 `git add .`，禁止笼统暂存整个目录。
3. 运行 `git diff --cached --name-status` 和 `git diff --cached --check`，确认没有无关文件、临时文件或格式错误。
4. 使用能准确说明本次改动的提交信息，不使用含糊的通用描述。
5. 推送 `main`。
6. 使用 `git rev-parse HEAD` 取得完整 40 位 SHA，并运行 `python tools/wait_for_pages.py <完整 SHA>`。
7. Pages 成功后，用带时间戳参数的程序化请求读取线上 `data.json`，核对目标条目、字段和数量，避免 CDN 缓存误判。
8. 再次运行 `git status --short`，确认工作区干净。

只有 Pages 成功且线上目标数据核对正确后，才能向用户表述“已发布”。

不得提交：`dist/`、`outputs/`、临时预览、工作簿备份、缓存或其他任务无关文件。

## 8. 工作流问题反馈

WorkBuddy 遇到流程缺陷、重复坑、规则歧义、工具故障或可能需要 Skill 的需求时，必须在当天 `.workbuddy/memory/YYYY-MM-DD.md` 追加：

```text
## 工作流问题反馈：<简短名称>
- 当前任务：
- 已遵循的规则或 Skill：
- 问题与复现条件：
- 临时处理及其风险：
- 对结果的影响：
- 相关文件、命令或错误证据：
- 建议整改方向：
- 是否属于 Skill 候选：是 / 否 / 不确定
```

记录问题不等于获得修改规则、绕开安全措施或创建 Skill 的授权。Codex 后续 review 时负责统一处理并验证整改结果。
